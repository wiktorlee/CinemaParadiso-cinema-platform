package pl.cinemaparadiso.enums;

/**
 * Role użytkowników w systemie
 * 
 * USER - zwykły użytkownik, może rezerwować miejsca
 * ADMIN - administrator, może zarządzać filmami, seansami, itp.
 */
public enum UserRole {
    USER,
    ADMIN
}

